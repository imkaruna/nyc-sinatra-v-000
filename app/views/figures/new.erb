<!DOCTYPE html>
<html>
<head><title>Create new figure</title></head>
<body>
<h1>Create New Figure</h1>
<form method="POST" action="/figures">
  <p>
    <label for="figure_name">Name: </label><br>
    <input type="text" id="figure_name" name="figure[name]" >
  </p>

  <p>
    <label>Title: </label><br>
    <% @titles.each do |title| %>
      <input type="checkbox" id="title_<%= title.id %>" name="figure[title_ids][]" value="<%= title.id %>"><%= title.name %><br>
    <% end %><br>
    <h3>Add New Title</h3>
      <label for ="new_title">New Title</label><br>
      <input type="text" name="title[name]" id="new_title"><br>
  </p>
  <p>
    <label>Landmark: </label><br>
    <% @landmarks.each do |landmark| %>
      <input type="checkbox" id="landmark_<%= landmark.id %>" name="figure[landmark_ids][]" value="<%= landmark.id %>"><%= landmark.name %><br>
    <% end %><br>
    <h3>Add New Landmark</h3>
      <label for ="new_landmark">New Title</label><br>
      <input type="text" name="landmark[name]" id="new_landmark"><br>
  </p>
  <p><input type="submit" name="submit" value="Create New Figure"/></p>
</form>
</body>
</html>
