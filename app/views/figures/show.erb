<!DOCTYPE html>
<html>
  <head>
    <title>Figures</title>
  </head>

  <body>
    <h1><%= @figure.name %></h1>
    <h3>Titles given:</h3>
    <% @figure.titles.each do |title| %>
    <li><%= title.name %></li>
    <% end %>
    <h3>Landmarks associated:</h3>
    <% @figure.landmarks.each do |landmark| %>
    <li><%= landmark.name %></li>
    <% end %>
    <h4><a href="/figures/<%= @figure.id %>/edit">Click here</a> to edit this figure</h4>

  </body>
</html>
